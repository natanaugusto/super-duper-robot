# Pantest

This is a Basic User API.

## Running the project

If you already have docker installed, you just need to run:

```bash
docker compose up -d
```

## Testing

You can run the unit tests by:

```bash
npm run test
```

And you can Import the `postman_collection.json` into your Postman.